var strFirstName = 'Kerry';
var strSecondName = 'Chung';


function output_fullName(firstName, secondName){
    return firstName +  ' ' + lastName;
    }
    
    var strFullName = output_fullName(strFirstName, strSecondName)
    
    console.log (strFullName)
    //notes - to add datatype info and predefine it
// blnVariable (True/Fase) 
// strVariable (String)
// objVariable (object)
// arrVariable (array)

//global variable is set outside of a function (generally you want to pass arguements within functions, don't normally use globals)
// local variable is declared as an arguement or within the function it's written in